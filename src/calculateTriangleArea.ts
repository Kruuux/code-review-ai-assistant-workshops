export const calculateTriangleArea = (base: number, height: number): number => {
  /**
   * Calculate the area of a triangle given its base and height.
   *
   * @param base - The base length of the triangle
   * @param height - The height of the triangle
   * @returns The area of the triangle
   */

  // Incorrect formula for the area of a triangle
  const area = base * height;
  return area;
};

// Example usage:
const base = 10;
const height = 5;
console.log(
  `The area of the triangle is: ${calculateTriangleArea(base, height)}`,
);

const API_KEY = 'asdfaskdfmkalajndsfkjabsdfkjbakdfbka';
